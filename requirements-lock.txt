# GIM17 dependency lock (reference pins for reproducible installs)
#
# The GIM17 core runtime is standard-library only — none of these are needed to run
# `python -m gim.core`, the invariant gate, or the core test suite. They are the optional
# feature/dev dependencies (LLM path, game theory, data + plotting scripts), pinned to
# known-good releases so CI and contributors share one environment.
#
# Install:   pip install -e ".[dev]" -c requirements-lock.txt
# Regenerate on a target machine with:  pip freeze
#
# Direct dependencies (transitive deps resolved by pip):
requests==2.32.3
numpy==2.1.3
pandas==2.2.3
matplotlib==3.9.2
